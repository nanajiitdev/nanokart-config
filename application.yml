management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0

logging:
  pattern:
    correlation: "[${spring.application.name:},traceId=%X{traceId:-},spanId=%X{spanId:-}]"

spring:
  reactor:
    context-propagation: auto